# SafeStadium AI - Local Development Environment Variables

# Server Configuration
PORT=3000

# Authentication (Used for mock JWT token generation)
JWT_SECRET=super_secret_local_key_123

# Database (Optional for local mock execution)
# If left commented out, the application will run using in-memory mock data.
# MONGO_URI=mongodb://localhost:27017/safestadium
